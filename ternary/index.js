const legalAge = 18
const personsAge = 16
const canDrive = (personsAge>=legalAge) ? "Yes, you can drive! Hope you have a driver license." : "Nope, you´re too young!"
console.log(canDrive);


const speedLimit = 130
const actualSpeed = 120
const speedCheck = (speedLimit<=actualSpeed) ? "You are going too fast! Slow down!" : "You are driving below the speed limit."
console.log(speedCheck);

const speedCheck2 = (speedLimit>=actualSpeed) ? "You are going too fast! Slow down!" : "You are driving below the speed limit."
console.log(speedCheck2);

const tooYoung = 19
const oldEnough = 21
const personAge = (tooYoung<=oldEnough)? "Here is some juice" : "Here is some wine."
console.log(personAge);

const itIsStudent = false
const isStudent = (itIsStudent)? "Ticket costs €5,00":"Ticket costs €12,00"
console.log(isStudent);

const morning = true
const timeOfDay = (morning) ? "Good Morning":"Good Evening"
console.log(timeOfDay);

 